import 'package:flutter/material.dart';

class HistoryRepairScreen extends StatelessWidget {
  const HistoryRepairScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('ประวัติ')),
    );
  }
}
